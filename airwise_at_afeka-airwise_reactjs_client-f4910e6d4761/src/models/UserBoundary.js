export class UserBoundary {
    constructor({ userId, role, username, avatar, email }) {
      this.userId = userId;
      this.role = role;
      this.username = username;
      this.avatar = avatar;
      this.email = email;
    }
  }